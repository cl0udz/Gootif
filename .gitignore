# macOS
.DS_Store

# Secrets — never commit real keys
config.php
agents/.env
agents/google-mcp.json

# Server runtime data (keep the deny-all .htaccess tracked)
data/*
!data/.htaccess

# Python
__pycache__/
*.pyc

# Claude Code local settings
.claude/settings.local.json
